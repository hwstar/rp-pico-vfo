#pragma once
// Compile options
#define CONFIG_ENABLE_MODE_SWITCHING /* Enable mode switching in the menu system */
#define CONFIG_DEFAULT_INITIAL_BAND_INDEX 0 /* Band table index selected at power up */
#define CONFIG_DEFAULT_STEP_SIZE_INDEX 3 /* initial step size index into step size table */
#define CONFIG_INITIAL_AGC true /* Initial agc state (true=enabled, false=disabled) on power up */

